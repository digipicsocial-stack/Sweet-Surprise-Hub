# SweetSurpriseHub Netlify Project

This is a reconstructed React frontend from the files exported from Emergent.

## Run locally
```bash
npm install
npm start
```

## Build for Netlify
```bash
npm run build
```
The project includes a frontend-safe mock product API because the original Emergent backend is not included in a Netlify static deployment.
